{% extends 'base.html.twig' %}

{% block title %}Payment index{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-12">
            <div class="card mb-4">
                <div class="card-header pb-0">
                    <div class="row">
                        <div class="col-6 d-flex align-items-center">
                            <h6 class="mb-0">Payment index</h6>
                        </div>
                        <div class="col-6 text-end">
                            <a href="{{ path('app_payment_new') }}" class="btn btn-outline-primary btn-sm mb-0">Create new</a>
                        </div>
                    </div>
                </div>
                <div class="card-body px-0 pt-0 pb-2">
                    <div class="table-responsive p-0">
                        <table class="table align-items-center mb-0">
                            <thead>
                            <tr>
                                <th class="text-uppercase text-center text-xxs font-weight-bolder opacity-7">Id</th>
                                <th class="text-uppercase text-center text-xxs font-weight-bolder opacity-7">BookingId</th>
                                <th class="text-uppercase text-center text-xxs font-weight-bolder opacity-7">PaymentId</th>
                                <th class="text-uppercase text-center text-xxs font-weight-bolder opacity-7">Amount</th>
                                <th class="text-uppercase text-center text-xxs font-weight-bolder opacity-7">PaymentMethod</th>
                                <th class="text-secondary opacity-7">actions</th>
                            </tr>
                            </thead>
                            <tbody>
                            {% for payment in payments %}
                                <tr>
                                    <td class="align-middle text-center">
                                        <span class="text-secondary text-xs font-weight-bold">{{ payment.id }}</span>
                                    </td>
                                    <td class="align-middle text-center">
                                        <span class="text-secondary text-xs font-weight-bold">{{ payment.bookingId }}</span>
                                    </td>
                                    <td class="align-middle text-center">
                                        <span class="text-secondary text-xs font-weight-bold">{{ payment.paymentId }}</span>
                                    </td>
                                    <td class="align-middle text-center">
                                        <span class="text-secondary text-xs font-weight-bold">{{ payment.Amount }}</span>
                                    </td>
                                    <td class="align-middle text-center">
                                        <span class="text-secondary text-xs font-weight-bold">{{ payment.PaymentMethod }}</span>
                                    </td>
                                    <td class="align-middle">
                                        <a href="{{ path('app_payment_show', {'id': payment.id}) }}" class="text-secondary font-weight-bold text-xs" data-toggle="tooltip" data-original-title="Edit user">
                                            Show
                                        </a>
                                        <a href="{{ path('app_payment_edit', {'id': payment.id}) }}" class="text-secondary font-weight-bold text-xs" data-toggle="tooltip" data-original-title="Edit user">
                                            Edit
                                        </a>
                                    </td>
                                </tr>
                            {% else %}
                                <tr>
                                    <td colspan="6">
                                        <p class="text-center">No Records Found</p>
                                    </td>
                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
